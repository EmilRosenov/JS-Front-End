function printResult(number, power) {
  function result(number, power) {
    console.log(Number(Math.pow(number, power)));
  }
  result(number, power);
}

printResult(2, 8);
printResult(3, 4);
